<!DOCTYPE html>
<html>
<head></head>

<body>



<div class="container">
<h2>Horaire</h2>

<ul>

    <% results.forEach(function(r) { %>
        <%= Object.keys(r) %>
        <%= JSON.parse(r) %>
        <li><%= r.nomemploye %> - <%= r.prenomemploye %></li>
    <% }); %>
</ul>

</div>





</body>
</html>